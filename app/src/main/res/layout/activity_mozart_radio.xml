<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_mozart_radio"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.olympiabardis.musicalstructureapp.MozartRadio">

    <ImageView
        android:id="@+id/album"
        android:layout_width="800dp"
        android:layout_height="400dp"
        android:src="@drawable/mozartalbum"/>

    <TextView
        android:text="@string/mozart_radio_instructions"
        android:textSize="10sp"
        android:gravity="center_vertical"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/songname"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_alignBottom="@id/album"
        android:textStyle="bold"
        android:textColor="#004D40"
        android:textSize="14sp"
        android:text="The Magic Flute by Wolfgang Amadeus Mozart"/>

    <ImageView
        android:id="@+id/musicplayer"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:src="@drawable/buttons"
        android:layout_below="@id/songname"/>

<TextView
    android:id="@+id/download"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/musicplayer"
    android:padding="5dp"
    android:textStyle="bold"
    android:textColor="#004D40"
    android:textSize="14sp"
    android:gravity="center"
    android:paddingBottom="2dp"
    android:paddingTop="2dp"
    android:text="DOWNLOAD >"/>

</RelativeLayout>
